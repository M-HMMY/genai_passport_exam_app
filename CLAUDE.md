# CLAUDE.md

このファイルは、このリポジトリで作業するときに最初に読む前提です。

## このアプリは何か

生成AIパスポート試験（GUGA＝一般社団法人生成AI活用普及協会）の学習アプリ。**教本を軸にした学習アプリ**であって、問題集アプリではありません。

**コードは `g_exam_app` から持ってきてあります。**`fe_exam_app` から数えて 5 つめにあたる姉妹アプリで、検査がいちばん充実しています。画面・記法・運用の作法は共通なので、**迷ったら向こうの実装を見れば答えがあります。**

**ただし向こうは別の試験です。**参照するのはコードと作法だけにして、試験の中身は持ち込まないこと。

**次の学習アプリを作るなら、先に [docs/handover.md](docs/handover.md) を読んでください。**このファイルは「このアプリをどう作るか」ですが、あちらは「**次のアプリをどう立ち上げるか**」を書いてあります。系譜 5 本ぶんの作り方・繰り返し出る誤りの型・Codex の回し方がまとまっています。

## 最優先の方針

**「通読できる教本」が最優先の要件です。** 演習機能より教本の質を優先してください。

その上で、**読者を取り違えると全部ずれます。**このアプリの読者は次の位置にいます。

> **IT の基礎語彙はあるが、技術者ではない。AI は「作る側」ではなく「使う側」。**

受験資格がなく、合格率は **8 割前後**。受けるのは AI を業務で使い始めた社会人、営業・企画・人事などの非技術職、学生です。「サーバ」「クラウド」は説明なしで通じますが、**「パラメータ」「潜在ベクトル」は通じません。**

**この試験に数式は出ません。**シラバスの詳細キーワードを全部見ましたが、数式を要求する項目は 1 つもありませんでした。**そのかわり英略語が大量に出ます**（AGI・ANI・RLHF・NLP・LLM・LM・RAG・MCP・MLM・NSP・VAE・GAN・RNN・LSTM・CNN）。

書くときの基準は次のとおりです。

- **英略語は、初めて出したその場で「正式名称・訳・何をするものか」の 3 点セットで書く。** これが最大の得点源であり、最大のつまずきどころです
- **数式を出さない。** 仕組みは図と例え話で説明する。`Diagram` は自由に使ってよい
- **モデル名・サービス名は「何ができるようになったか」とセットで書く。** 名前だけ並べても覚えられません
- **たとえ話を最初に置く。** 抽象的な定義はそのあと
- **カタカナ語には、なぜその名前なのかを添える。** 「アテンション（attention＝注意。どこに注目するかを重みで決める）」

判断に迷ったら「**AI を使い始めたばかりの非技術職が、飛ばさずに読み切れるか**」を基準にしてください。

## 試験の形（教本の前提）

**2026 年 9 月 13 日に [GUGA 公式](https://guga.or.jp/outline/) で再確認した内容です**（9 月 10 日の初回確認から変更なし）。要項は改訂されるので、作業を始める前にもう一度見てください。教本の中でも断定せず、「最新の情報は GUGA の公式サイトで確認」と促してください。

| 項目 | 内容 |
| --- | --- |
| 形式 | **四肢択一式（一部複数選択を含む）**。記述も実技もない |
| 出題数 | **60 問** |
| 試験時間 | **60 分**（1 問あたりちょうど 60 秒） |
| 実施 | **オンライン（IBT）年 5 回**（2・4・6・8・10 月）。**各回とも受験期間が 1 か月ある** |
| 受験資格 | なし |
| 受験料 | 一般 11,000 円 / 学生 5,500 円（税込） |
| 合格基準 | **非公表** |
| 合格率 | **79.90 %**（2026 年 6 月試験。累計受験者 115,062 名） |
| 資格の有効期限 | なし（無期限）。ただしシラバス改訂時に**資格更新テスト**が別途ある |
| シラバス | **2026 年 2 月試験より適用**の版。第 1 章〜第 5 章 |

書き起こしを [docs/syllabus-2026-02.md](docs/syllabus-2026-02.md) に置いてあります（中項目 23 個・詳細キーワード約 200 語の全文と、この教本の章との対応表）。章を書くときはこれを読ませてください。ただし**シラバスは改訂されます。**版が上がっていないか公式サイトで確かめること。

### この試験の形が設計に効く 4 点

**1. 1 問あたりちょうど 60 秒。**

**じっくり考え込む余裕はありません。**とはいえ、計算も長文読解も出ないので、知っていれば即答できる問題がほとんどです。

落とすとしたら**用語の取り違え**です。似た名前が大量に並ぶ試験なので、一問一答（` ```quiz `）と用語辞典の価値が高くなります。**問題文を長くしないこと。** `npm run check` が、空白を除いて 120 字で注意・160 字でエラーにします。

**2. 合格基準が非公表。**

合格率は 8 割前後と高いのですが、**GUGA は合格基準を公表していません。**7 割とも 8 割とも言われますが、いずれも非公式です。

`src/pages/Mock.tsx` は `g_exam_app` から引き継いでいて、**合否を判定せず、点数への換算もしません。**出すのは次の 3 つだけ。

- 正答率（「合格圏」などの判定は出さない）
- **時間内に解き切れたか**（未解答数と、1 問あたりの実測秒数を持ち時間と比べる）
- 分野別・章別の正答率（どこに時間を使うか決めるため）

**非公表の基準を、アプリが勝手に「合格」と断定しないこと。**この方針を崩す変更を入れないでください。「合格率 8 割だから大丈夫」という書き方もしないこと。

出題数の内訳も**公表されていません。**模試の構成比は、シラバスの詳細キーワード数から按分した推定値（`categories.ts` の `questions`）です。**本番の内訳ではないと、コメントにも画面にも書いてあります。**

**3.「一部複数選択を含む」— 対応済み。**

公式の出題形式は「四肢択一式（**一部複数選択を含む**）」です。**`Question.answer` は、添字ひとつでも添字の配列でも書けます。**配列にすると複数選択の問題になります（例: `answer: [0, 2]`）。

**読むのも比べるのも `src/lib/answer.ts` を通すこと。**画面ごとに場合分けを書くと、採点が画面ごとにずれていきます。**選択の状態は、単一選択でも配列で持ちます**（未選択は空配列）。`number | null` と `number[]` を使い分けると、未選択の表し方が 2 通りになって必ず取りこぼします。

**採点は完全一致だけを正解とします。**GUGA は採点方法を公表していないので、部分点の規則を決めたらそれはこちらの創作になります。合否を判定しないのと同じ理由で、決めません。

**いくつ選ぶのかは画面に出しません。**本番がそれを教えてくれる保証がないためです。かわりに**問題文へ「2 つ選びなさい」と書きます。**`npm run check` が書き忘れをエラーにします。

**4. 中身が半年で古くなる。**

第 2 章と第 3 章は**モデル名とサービス名の塊**です。GPT-5、Sora、Operator、Codex、Gemini、Claude、Copilot、Veo3、GenSpark、Manus、Skywork AI。

**「いつ時点の話か」を本文に必ず書いてください。**姉妹アプリから引き継いだ教訓ですが、生成 AI が主題のこのアプリではさらに重くなります。

## 出題範囲（シラバス 2026 年 2 月版）

**公式 PDF を確認済みです。**5 章構成で、章ごとの重みは次のとおり。

| 章 | 名前 | 中項目 | キーワード | 推定出題数 |
| --- | --- | --- | --- | --- |
| 第 1 章 | AI（人工知能） | 5 | 39 | 11 |
| 第 2 章 | 生成AI（ジェネレーティブAI） | 3 | 62 | 17 |
| 第 3 章 | 現在の生成AIの動向 | 4 | 20 | 7 |
| 第 4 章 | 情報リテラシー・基本理念とAI社会原則 | 6 | 65 | 19 |
| 第 5 章 | テキスト生成AIのプロンプト制作と実例 | 5 | 16 | 6 |

**「推定出題数」はキーワード数から 60 問を按分しただけの目安です。**GUGA は内訳を公表していません。

**第 5 章はキーワード数では測れません。**詳細キーワードは 16 語しかありませんが、**学習項目が 30 個以上**並びます。「用語を覚える」章ではなく「やり方を知っているか」を問う章なので、按分値を上乗せしてあります。

### 章立て（確定）

**公式の 5 章は、そのまま `FIELDS`（分野）にしてあります。**アプリの「章」（`CATEGORIES`）は、1 ファイルが大きくなりすぎないよう公式の章を割ったものです。

| 章 ID | 章名 | シラバス | 推定出題数 |
| --- | --- | --- | --- |
| `intro` | はじめに | 範囲外 | — |
| `a-ai` | AI（人工知能）の基礎 | 第 1 章 全 5 中項目 | 11 |
| `b-model` | 生成 AI が生まれるまで | 第 2 章 中項目 1 | 9 |
| `c-chatgpt` | ChatGPT と主要な生成 AI | 第 2 章 中項目 2〜3 | 8 |
| `d-service` | いまの生成 AI でできること | 第 3 章 中項目 1〜2 | 4 |
| `e-agent` | RAG と AI エージェント | 第 3 章 中項目 3〜4 | 3 |
| `f-security` | インターネットリテラシーとセキュリティ | 第 4 章 中項目 1〜2 | 5 |
| `g-privacy` | 個人情報保護 | 第 4 章 中項目 3 | 3 |
| `h-rights` | 制作物に関わる権利 | 第 4 章 中項目 4 | 4 |
| `j-principles` | AI 社会原則・ガイドラインと AI 新法 | 第 4 章 中項目 5〜6 | 7 |
| `k-prompt` | LLM とプロンプトの基礎 | 第 5 章 中項目 1〜2 | 3 |
| `l-business` | プロンプトの実践とビジネス応用 | 第 5 章 中項目 3〜5 | 3 |

**`i-` を章 ID に使っていません。**入門編の節 ID が `i-1`〜`i-4` なので、紛らわしさを避けて `h` の次を `j` にしてあります。

`src/data/textbook/<章 ID>.ts` と `src/data/questions/<章 ID>.ts` が 1 章 1 ファイルで対応します。**複数のエージェントを並行で走らせても、1 ファイル 1 担当なら衝突しません。**

### 2026 年 2 月改訂で追加された項目（優先度が高い）

**新しく加わった項目は出題されやすい**ので、薄い章でも手を抜かないこと。

- **RAG**（`e-agent-1`）… RAG とは / 歴史と発展 / 仕組みとメリット / ユースケース
- **AI エージェント**（`e-agent-2`）… とは / 仕組み / ツール事例（GenSpark・Manus・Skywork AI）/ MCP と外部連携
- **AI 新法**（`j-principles-3`）… 人工知能関連技術の研究開発及び活用の推進に関する法律。**2025 年 6 月 4 日公布**
- **AI 事業者ガイドラインが第 1.0 版 → 第 1.1 版**に改訂されたことによる更新
- **第 2 章のモデル追加**… GPT-o1, GPT-o3, GPT-o4, GPT-4.1, GPT-5, Sora, Operator, Codex, Image Generation, Gemini, Claude, Copilot

## 変えないこと

- **ランタイム依存は React だけ。** ルータ・Markdown・数式・図はすべて自前。ライブラリを足す前に、本当に必要か検討する
- **外部通信をしない。** 学習記録は localStorage のみ。サーバもアカウントもない。**第 5 章がプロンプトの章なので誘惑がありますが、生成 AI の API を叩く機能は作りません**
- **オフラインで動く。** PWA として成立させる
- TypeScript は strict（未使用変数もエラー）

## 節の書き方

本文は Markdown サブセットです。1 つの節は次の骨格で書きます。

```
# ざっくり言うと          ← 日常語で「何の話か」
（本論。見出し・表・図）
> **試験のポイント** …     ← 暗記すべき事項
> **よくある勘違い** …     ← 先回りして潰す誤解
# この節のまとめ           ← 箇条書き 5〜6 個
```quiz                    ← 一問一答（採点も記録もしない）
問い :: 答え
```
```

「この節のまとめ」「試験のポイント」「よくある勘違い」の 3 つは、`src/lib/digest.ts` が自動で抜き出して**直前チェックシート**に並べます。**書式を崩すと拾われません。**

**この試験は用語の取り違えで落とすので、` ```quiz ` を厚めに入れてください。**とくに**英略語の正式名称**は一問一答向きです。`npm run check` が、骨格の 3 つ（「# ざっくり言うと」「# この節のまとめ」「> **試験のポイント**」）と一問一答の数（5 問未満で注意）を数えます。

### 使える記法

`g_exam_app` と同じです。`compare` 図は「1 行 1 セル、偶数行が左・奇数行が右」で書きます（要素が奇数だと `npm run check` がエラーにします）。図の中では `**強調**` が効きません。

**数式は使いません。**`src/lib/math.tsx` と `mathSymbols.ts` は骨格として残してありますが、**このアプリの本文で `$...$` を使う場面はないはずです。**使いたくなったら、たいてい説明の仕方が技術寄りに寄りすぎています。

### バックスラッシュの落とし穴（重要）

本文は TypeScript の**テンプレートリテラル**の中にあります。

| 書きたいもの | ファイルに書く形 |
| --- | --- |
| `\n` を文字として出す | `\\n` |
| バックティック | バックスラッシュ + バックティック |
| `${` | `\${` |

さらに、**bash のヒアドキュメント（`cat > file <<'EOF'`）はバックスラッシュを勝手に落とします。** 姉妹アプリで何度も壊しました。Write ツールを使うか、`scripts/emit_textbook.py` を使ってください。

## 問題の書き方

- **`sectionId` は必ず付ける。** 解説から教本へ戻る導線と、節ごとの出題に使う
- 選択肢は 4 つ。**正解の位置は散らす**
- 解説では、正解の理由だけでなく**誤答がなぜ誤りか**にも触れる
- **誤答も正解と同じ密度で書く。** 正解だけが長いと、読まずに「長いものを選ぶ」で当てられます
- **「必ず」「すべて」を誤答専用にしない。** 言い切りが誤答側にしか出てこないと、それ自体が手掛かりになります
- **問題文を長くしない。** `npm run check` が、空白を除いて 120 字で注意・160 字でエラーにします

上の 3 つは `npm run check` が数えて注意を出します。

## 作業の作法

### 必ず通すもの

```
npm run check   # データの整合性 + 実際に描いてみる検査
npx tsc --noEmit
npm run build
```

`npm run check` は「型では防げない食い違い」を捕まえます。ID 重複、参照切れ、記法の崩れ、図の書式、リンクの飛び先、ドリルの選択肢、節をまたいだ問題の重複、そして**実際に React で描画して NaN や崩れが出ないか**まで見ます。**新しい不整合の型を見つけたら、直すついでに `scripts/check.ts` へ検査を足してください。**

### 内容の正しさ

**書いた本人が読み返しても間違いは見つかりません。** 系譜での実績は次のとおりです。

| アプリ | 対象 | 別の目で見つかった誤り | 自己レビューで見つかった数 |
| --- | --- | --- | --- |
| `fe_exam_app` | 教本 約 7 万字 | 16 件（重大 4 件） | 0 件 |
| `itpassport_exam_app` | 教本 52 節 | **90 件超（重大な事実誤り 30 件）** | 0 件 |
| `itpassport_exam_app` | 確認問題 334 問 | **94 件** | 0 件 |
| `g_exam_app` | 教本 59 節 | **200 件超**（2 回に分けて実施） | 0 件 |

まとまった量を書いたら、**書いた人とは別の目で検証する工程を必ず入れてください。**省略できる工程ではありません。

**このアプリに固有の検証観点が 3 つあります。**

1. **情報が古くなっていないか。** 第 2 章・第 3 章はモデル名とサービス名の塊です。**「いつ時点の話か」を本文に書いてください**
2. **法律・ガイドラインが現行のものか。** AI 新法、AI 事業者ガイドライン（第 1.1 版）、個人情報保護法、著作権法。**年数と条番号は、書いた人以外が一次資料で確かめること**
3. **原則の「数」が合っているか。** 第 4 章には「基本理念」「AI 社会原則」「共通の指針」という**似た名前の一覧が 3 つ**あり、それぞれ数が違います。`npm run check` の「図の題の『N つ』と実際の数」の検査が効きますが、本文の地の文は機械では拾えません

### 系譜で実際に出た誤りの型

**同じ型は繰り返し起きるので、書くときも直すときも意識してください。**

教本で出た型：

- **一度確かめた事実が、あとから古くなっていた** … 姉妹アプリで、確認して書いた法令の施行日が、**施行の 6 日前に延期されていた。「確認済み」と書いた事実にも期限がある**
- **法改正の年をまたいだ誤り** … 改正前の年数を書いていた
- **「まとめて言い切る」ことで生まれる誤り** … 4 つの権利を一律に説明したが、1 つだけ例外があった
- **条文の要件の書き落とし** … 著作権法 30 条の 4 から「又は他人に享受させる」が抜けていた
- **節をまたいだ食い違い** … 同じ用語を、章によって違う説明にしていた
- **本文と図の食い違い** … 図の軸が本文と違っていた
- **`compare` 図の誤用** … 1 対 1 の対応表のつもりで書いた行が、偶数行が左・奇数行が右に振り分けられて意味が壊れる。**要素数が偶数だと検査を素通りする。系譜で 4 回起きている**
- **`stack` 図の向きが意味と逆** … `stack` は**先頭が最も幅広い**
- **本文がソースの行番号を指していた** … 「最大の問題は 3 行目です」。`compare` は左右に振り分けられるので読者の画面に 3 行目はない
- **「よくある勘違い」が勘違いでない** … 事実の再掲になっていた。`digest.ts` が直前チェックシートに拾うので影響が広い
- **説明なしの初出** … 定義より前に用語が出ていた

問題で出た型：

- **誤答の説明もれ** … 4 つのうち 1 つだけ解説で触れていない。**最も多い型**
- **誤答が実は正しい** … 言い切りすぎて誤答にしたつもりが、実際には正しかった
- **分類の軸が混ざっている** … 誤答が正解と別の軸の言葉で、両方成り立ってしまった
- **かっこ書きの言い換えが別語だった** … 同義語だと思って添えた語が、別の意味の専門用語だった
- **語源の思い込み** … もっともらしいが違う由来を書いていた
- **条件の列挙もれ** … 成立条件が 1 つ落ちていた
- **章をまたいだ重複** … 1 節ずつ見ている限り気づけない
- **正解だけが突出して長い** … 読まずに「長いものを選ぶ」で当てられる
- **正解の位置の偏り** … 姉妹アプリでは「エ」が 288 問中 3 問だったことがある。**ただし数値や順序が選択肢になっている問題は並べ替えない**

逆に、**レビュー側が範囲外の指摘をしてくる**こともあります。**プロンプトでこちらが例示した語が、そのまま誘導になります。採否はこちらで判断してください。**

なお、**「解説が誤答に触れているか」を文字列一致で機械的に測るのは無理です。**言い換えて触れているものを全部見逃しと数えてしまいます（334 問中 233 問が誤検出でした）。同じ検査を作り直さないでください。

### 公開のルール

公開リポジトリにする前提です。

- **コミットの署名は GitHub の noreply アドレスを使う。** 実メールアドレスはコミット履歴に永久に残ります
- 個人情報・ローカルパス・API キーを含めない
- **`.github/workflows/deploy.yml` の対象ブランチを、実際のブランチ名に合わせること。** ITパスポート版では `main` を待つ設定なのに既定ブランチが `master` で、**一度も動いていませんでした**。このリポジトリは `main` に合わせてあります

## 章を書かせるやり方

**Codex CLI を使います**（`npm install -g @openai/codex`、ChatGPT ログイン済み）。姉妹アプリの教本の大半はこれで書きました。

```bash
codex exec --sandbox workspace-write --color never -o out.txt - < scripts/prompts/03-xxx.md
```

**★ リポジトリの直下で走らせてください。**`--cd` を付けていないので、別の場所から叩くと、そのディレクトリを読みにいきます。
（公開リポジトリにローカルパスを残さないため、2026 年 9 月 20 日に系譜全体から外しました。）


依頼側のコンテキストを食わないよう、`-o` で最終メッセージだけをファイルに落とすのが要点です。**実際に使ったプロンプトは `g_exam_app/scripts/prompts/` と `itpassport_exam_app/scripts/prompts/` に全部残っています。そこの README を読んでひな形にしてください。**何をプロンプトに入れると品質が上がるか（間違えやすい事実の名指し、リンクしてよい節 id の全列挙など）が書いてあります。

**章のプロンプトには [docs/syllabus-2026-02.md](docs/syllabus-2026-02.md) の該当箇所を読ませてください。**その章が担当する中項目の「学習目標」「学習項目」「詳細キーワード」が、そのまま書くべき内容の外枠になります。

レビューは `90-review.md`（教本用）と `91-review-questions.md`（確認問題用）を対で用意します。**教本用をそのまま問題に使わないでください。**教本は「書いてあることが正しいか」ですが、問題は「**正解が本当に 1 つに定まるか**」が最大の論点で、見る点が違います。

**Codex には利用上限があります。** ITパスポート版では、複数ジョブを並行させて上限に当たり、**ファイルを編集し終えた直後・報告を書く前に停止した**ことがあります。編集は残るのに報告が残らないので、何が終わっていて何が終わっていないか分かりません。**上限に当たったら、`grep -c` などで期待する語を数えて、実際に反映されたかを確かめてください。**

## いまの状態

| 項目 | 状況 |
| --- | --- |
| シラバスの確認 | **済**（2026 年 9 月 13 日に再確認）。公式 PDF を [docs/syllabus-2026-02.md](docs/syllabus-2026-02.md) に書き起こし済み。**版は「2026 年 2 月試験より適用」のまま**で、改訂の予告もなし。書き起こしは詳細キーワード 183 語のうち 180 語を現物 PDF と機械照合して一致を確認した（残り 3 つは「（記載なし）」という埋め草） |
| 試験要項の確認 | **済**（2026 年 9 月 13 日に再確認）。60 問 / 60 分 / IBT 年 5 回 / 合格基準非公表。**変更なし** |
| 骨格の移植 | **済**。`npm run check` / `npx tsc --noEmit` / `npm run build` の 3 つが通る |
| 章立て | **確定**（12 章）。`src/data/categories.ts` |
| 節割り | **確定**（全 41 節）。[docs/section-plan.md](docs/section-plan.md) |
| 模試の合否判定 | **`g_exam_app` から引き継ぎ済**。合否を判定せず、正答率・解答ペース・分野別正答率だけを出す |
| 教本の本文 | **済**（2026 年 9 月 11 日）。全 41 節・約 14.9 万字。入門編は手書き、第 1〜11 章は Codex に 1 章 1 担当で書かせた |
| 章ごとの執筆プロンプト | **済**。`scripts/prompts/`。共通部分は `00-common.md`、章ごとが `02`〜`12`、レビューが `90`・`91`、確認問題が `20` |
| 教本の通しレビュー | **2 巡済**（2026 年 9 月 11 日）。1 巡目は Claude、2 巡目は `90-review.md` を 5 ジョブに割って Codex。2 巡目だけで **105 件**の指摘。著作権法 30 条の 4 から「又は他人に享受させる」が抜けていた、IBT の受験端末をパソコンに限定していた、Operator を現役サービスとして書いていた、**シラバスの「AI 社会原則 6 項目」を 2019 年の原文（7 原則）そのものとして書いていた**など |
| 確認問題 | **済**（2026 年 9 月 13 日）。**193 問**（本番 60 問の 3 倍超）。**うち 13 問が複数選択**で、正解の位置は ア 6 / イ 7 / ウ 6 / エ 7。残る 180 問は四肢択一で、正解の位置は ア 44 / イ 44 / ウ 46 / エ 46。**教本を書いたのは Codex、問題を書いたのは Claude で、目は分けてある** |
| 確認問題のレビュー | **済**（2026 年 9 月 11 日）。`91-review-questions.md` を 4 ジョブに割って Codex。**68 件**の指摘。正解が 2 つあった問題（限定提供データの要件、Adobe Firefly の動画対応）を含む |
| 計算ドリル | **作らない方針**（この試験に計算問題は出ない）。`src/data/drills.ts` は骨格として残してある |
| 体験ウィジェット | **済**（2026 年 9 月 11 日）。8 つ（personal-info / rights / phishing / tokenize / temperature / attention / prompt-parts / few-shot）。`Tools.tsx` の `GROUPS` と `render-check.tsx` の `WIDGETS` に登録済みで、教本の該当節にも埋め込んである |
| アイコン | **済**。`scripts/make_icons.py` で `public/icons/` と `scripts/app.ico` をまとめて作る。**紫→藤色に白い「AI」**（姉妹アプリと色がぶつからないように選んである） |
| 複数選択問題への対応 | **済**（2026 年 9 月 12 日）。`answer` を配列にし、`ChoiceList` と採点側（`Practice` / `Review` / `Mock`）をまとめて直した。上記「この試験の形が設計に効く 4 点」の 3 番を参照 |
| 複数選択のレビュー | **済**（2026 年 9 月 13 日）。`92-review-multi.md` を新設し、Codex に掛けた。最初の 8 問は 4 巡（採用した指摘は 24 → 8 → 4 → 1）、あとから足した 8 問はさらに 2 ジョブ。**1 巡目で 8 問中 6 問が既存問題の焼き直しと判明**し、2 巡目では書き直した版から「誤答が実は正しい」が 3 件出た |
| デスクトップのショートカット | **済**（2026 年 9 月 13 日）。`scripts/make_shortcut.ps1`。下の「デスクトップのランチャー」を参照 |
| git | **公開済**（2026 年 9 月 12 日）。`https://github.com/M-HMMY/genai_passport_exam_app`。既定ブランチは `main` で、`deploy.yml` も `main` に合わせてある |
| 実ブラウザでの動作確認 | **済**。`node scripts/drive.mjs`（`npm run dev` を別に立てておく）。Edge を headless で起動し、Chrome DevTools Protocol で実際に押す。**追加インストールなし**（Node 24 の組み込み WebSocket を使う）。これで学習記録の二重登録バグが見つかった |
| 引き継ぎ書 | **済**（2026 年 9 月 12 日）。[docs/handover.md](docs/handover.md)。**次のアプリを作るときはここから** |

## 次にやること

順番に意味があります。

1. **複数選択の問題を、さらに増やすかどうかを決める** … いまは 193 問中 13 問です。本番の「一部」がどれくらいかは公表されていないので、**これ以上増やす根拠はありません。**増やすなら**新しい判断を求められる角度があるかどうか**で決めること。数を目標にすると焼き直しになります（実際になりました）。`c-chatgpt` に複数選択がないのは意図的で、あの章は名前と提供元の対応が中心なので、どう組んでも既存 2 問の合成にしかなりませんでした。**足したら必ず `scripts/prompts/92-review-multi.md` に掛けること**（下の注意を参照）
2. **次の改訂を待つ** … 2026 年 9 月 13 日の再確認では、要項・シラバス・合格率・AI 事業者ガイドラインの版のいずれも変わっていませんでした。**次に見るべきもの**は、(a) 2026 年 8 月試験の結果（9 月 13 日時点で未公表。6 月試験は月末終了から約 2 週間後に出ています）、(b) シラバスの次版、(c) AI 事業者ガイドラインの第 1.3 版です

**レビューで出た「シラバスと一次資料が食い違う」型に注意。**AI 社会原則は、シラバスが 6 項目として挙げるのに対し、2019 年の原文「人間中心の AI 社会原則」は **7 原則**で名前も違います。**試験で答えるのはシラバスの側**ですが、**教本が一次資料の内容として断定してはいけません。**同じ型が、AI 事業者ガイドラインの版（シラバスは第 1.1 版、現行は第 1.2 版）にもあります。

**直したら、直した版をもう一度見せること。**複数選択の 8 問では、1 巡目の指摘を反映して書き直した版に 2 巡目を掛けたら、**書き直したところから「誤答が実は正しい」が 3 件**出ました。1 巡で終わりにしていたら、全部残っていたことになります。

**あとから足す問題は、既存問題の焼き直しになりやすい。**定義を問う問題は最初の一巡で出そろっているので、**具体的な場面へ当てはめる形**にしてください。**この重複は機械では拾えません**（言い回しが違うので 2-gram の検査を素通りします）。レビューのプロンプトで拾うしかない型です。

**直した結果、別の型を作り込むことがあります。**「正解 2 つが同じことの言い換え」を直そうとして**「事例 1 つ＋その目的」**という組み方にしたら、今度は**分類の軸が混ざりました。**設問が「水増しに当たるものはどれか」なのに正解の片方が「水増しを行う目的」では、述語を厳密に取れば正解は 1 つです。**正解 2 つは、同じ軸の上で互いに独立していること。**

**Codex の利用上限に注意。**5 本を並行させると当たります。**当たるとファイルは書き換わっているのに `-o` の報告だけが残らない**ので、`git diff` と `grep -c` で検算してください。同時に走らせるのは 4 本までが無難です。

### デスクトップのランチャー

**`scripts/launch.cmd` → `scripts/launch.ps1`** の 2 段構えです。`g_exam_app` から引き継いでいます。
**ショートカットは `scripts/make_shortcut.ps1` で作ります**（2026 年 9 月 13 日に作成・動作確認済み）。

```
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\make_shortcut.ps1
```

- **`launch.cmd` は ASCII だけで書くこと。** cmd は `.cmd` を OEM コードページ（日本語環境では cp932）で読むので、日本語を書くと文字化けします。**表示する文字列は `launch.ps1` 側に置いてあります**
- **`launch.ps1` は BOM 付き UTF-8 で保存すること。** Windows PowerShell 5.1 は BOM のない `.ps1` を cp932 として読むため、**BOM を落とすと日本語の行で構文エラーになります**（実際に踏みました）
- ショートカットは `WScript.Shell` で作ります。**作成後は読み直して `IconLocation` を確認すること。** パスの区切りがエスケープとして消費され、壊れたパスが入ることがあります（これも実際に踏みました）。`make_shortcut.ps1` は最後に読み直して、起動するもの・作業フォルダ・アイコンの 3 つを照合します
- **ランチャ自体も一度は走らせること。**このリポジトリでは長らく一度も起動していませんでした。`npm run preview` を 4173 番で立てて、ブラウザを開くところまで実際に動きます（2026 年 9 月 13 日に確認）

### iPhone で使う

`g_exam_app` で整えたものをそのまま引き継いでいます。

- **PWA の資産は揃っています** … `manifest.webmanifest`、`sw.js`、`public/icons/`（**透過なしの正方形**。iOS は透明な画素を黒で塗るため）
- **`theme-color` は明暗の 2 本立て** … iPhone のステータスバーの背景になるので、画面上端の `.topbar`（`--surface`）と同じ色にしてある
- **オフライン保存は HTTPS でのみ有効** … 同じ Wi-Fi の `http://192.168.x.x:5173` でも閲覧はできますが、Service Worker は動きません。**ホーム画面に入れて使うには公開が要ります**

---

## ★ 8 つめから配った道具で測った結果（2026 年 9 月 20 日）

**`kouatsugas_otsu_exam_app` で作った検査と道具を、このリポジトリにも入れました。**走らせた結果を残しておきます。

| 測ったもの | 結果 |
| --- | --- |
| 解説と `answer` の食い違い | **0 件**（検査を `scripts/check.ts` に入れました） |
| 正解位置の偏り | 23.9 / 25.6 / 25.6 / 25.0 %。**問題なし** |
| 確認問題が使っていて教本に無い語 | **0 件** |

**★ 選択肢を並べ替えてはいけません。****180 問すべての解説が、選択肢を位置の記号で指しています。**

**★ 複数選択の問があるので、`answer` が配列になります。**新しい検査はそこを飛ばしています（`src/lib/answer.ts` を通すという約束も見ること）。

**道具の使い方。**

```
node scripts/echoes.mjs <語>   # その語がどこに何回あるかを、居場所つきで出す
node scripts/terms.mjs         # 確認問題が使っていて、教本に無い語を挙げる
```

**★ 直す前に `echoes.mjs` を走らせてください。****1 つの主張は、1 つの節の中に 6 つの居場所を持ちます**（本文・表・試験のポイント・よくある勘違い・この節のまとめ・一問一答）。**本文だけ直すと 5 つ残ります。**
