import type { Question } from '../../types';

/**
 * 第 3 章 現在の生成AIの動向：いまの生成 AI でできること（categoryId: 'd-service'）の確認問題。**未執筆。**
 * sectionId は必ず付ける（解説から教本へ戻る導線に使う）。
 */
export const dServiceQuestions: Question[] = [];
